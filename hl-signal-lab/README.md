# HLシグナルラボ — Hyperliquid 通知bot & 条件検証ツール

Hyperliquidの**全Perp銘柄（HIP-3ビルダー銘柄含む）**をブラウザ1枚で監視し、条件発火を**Discordに通知**、さらに過去のローソク足で**売買条件の成績を検証**できるツールです。サーバー不要・単一HTMLファイル。発注機能はありません（監視・検証のみ）。

## 機能

- **スキャナー**: 全銘柄の価格 / 15分・1時間・24時間変動 / Funding（時率＋年率換算）/ 現物乖離 / OI / 出来高を自動更新。HIP-3銘柄は金色タグ表示
- **アラート（9種）**: 急変動・24h変動・Funding率・Funding符号反転・現物乖離・OI急変・清算カスケード検知・対BTC相対強弱・価格レベル到達。確度スコア（S/A/B/C級）付きでDiscordへ、エントリー/損切り/利確の参考プラン付き
- **戦略一括スキャン**: 直近1週間〜3ヶ月を選び、内蔵全プリセットのロジックを選択銘柄群に一括適用して成績順にランキング。「今の相場に合う型」を探す羅針盤
- **バックテスト**: 8条件 × 順張り/逆張り × SMAフィルタ × エントリー遅延（N本待機）× 損切り/利確 × **レバレッジ（清算を−100%として計上）** × 手数料込み。結果はワンクリックでDiscordに要約送信
- **プリセット16種（2026年8月刷新版）**: 低頻度化・ATR幅・ロング偏重・時間帯効果（米国時間/週明け）・メイカー前提の明示、で全面再設計。**マイプリセット**として自分の設定を保存・編集・削除でき、一括スキャンにも自動で含まれる
- **時間帯フィルタ**: 米国時間・アジア・欧州・週明け（Monday Asia Open効果）でシグナルを絞り込み可能
- **ログ強化**: ルール別・確度別・銘柄でのフィルタと全列ソート、絞り込み反映CSV
- **Python bot連携**: 「🤖 bot用設定を書き出す」→ botフォルダのhl_bot.pyがそのまま実行（paper→testnet→本番の安全ラダー付き）
- **SL/TP最適化**: ロジック確定後、損切り×利確の全組み合わせをグリッド探索。データを前半70%（探索）/後半30%（検証）に分割し、単独ピークでなく「台地」（3×3近傍平均）を優先してランキング。ヒートマップ表示、ATR倍率方式（変動適応）対応
- **ポジション監視（清算警報）**: ウォレットの公開アドレスを登録すると、保有Perpの清算価格・実レバ・uPnLを無料APIで自動取得。清算まで残り7%で警報、3.5%でS級緊急通知（@here対応）。秘密鍵は不要・入力禁止
- **ハートビート**: 1時間ごとの生存レポートをDiscordへ（レポートが途絶＝ブラウザ停止のサイン）

## GitHub Pagesで公開する手順（5分）

1. GitHubで新しいリポジトリを作成（例: `hl-signal-lab`）。PublicでもPrivateでも可（Pages無料公開はPublic）
2. `index.html` と `README.md` をアップロード（Add file → Upload files）
3. リポジトリの **Settings → Pages** を開く
4. **Source: Deploy from a branch / Branch: main / フォルダ: /(root)** を選んで Save
5. 数分後に `https://<ユーザー名>.github.io/hl-signal-lab/` で公開されます

以後の更新は `index.html` を上書きアップロードするだけです。

## Discord通知の設定

1. Discordで通知用サーバー（またはチャンネル）を用意
2. **サーバー設定 → 連携サービス → ウェブフック → 新しいウェブフック** → チャンネルを選び **URLをコピー**
3. ツールの**設定タブ**にURLを貼り付け → **テスト送信** で確認 → 「アラートをDiscordに送信する」をON
4. スマホのDiscordアプリで当該チャンネルの通知をONにすれば、外出先でもプッシュで受け取れます

> ⚠️ **Webhook URLは絶対にコードやリポジトリに書き込まないでください。** URLを知っている人は誰でもそのチャンネルに投稿できます。URLはブラウザのローカル保存のみで、GitHubには一切送信されません。

## 運用の前提と制約

- 監視は**このページを開いている間だけ**動きます。自宅PC（またはタブレット）でページを開いたままにし、通知はDiscord経由でスマホに受け取る運用を想定しています。ヘッダーの「画面スリープ防止」をONに
- ローソク足はHyperliquid APIの仕様で**各時間足あたり直近約5000本**まで（1時間足≈200日分）
- **HIP-3ビルダー銘柄の取引手数料は通常銘柄の約2倍**です。バックテストの手数料欄を上げて検証してください
- ビルダーDEXを多数有効化する場合はレート制限対策として**更新間隔15秒以上**を推奨
- 現物（スポット）は未対応

## 免責

本ツールおよび内蔵プリセットは情報提供であり、投資助言ではありません。過去の成績（バックテスト結果を含む）は将来の成果を保証しません。プリセットは「検証の出発点」であり、利用前に必ずご自身で直近データ・分割期間での再検証を行ってください。売買判断と資金管理はご自身の責任で行ってください。

## 次のステップ（自動売買へ）

このツールで有望と確認できた条件は、Hyperliquid API（発注系）を使うPythonボットにそのまま移植できます。VPS上で24時間稼働 → 少額でのライブ検証 → 段階的増額、が推奨手順です。
